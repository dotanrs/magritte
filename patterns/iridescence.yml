# Create vibrant dynamic range of colors
steps:
  - name: recursively update the colors
    command: "loop-rgb 5 = (mod(R * 1.17 + G * 0.31 + 17, 256), mod(G * 1.13 + B * 0.29 + 31, 256), mod(B * 1.11 + R * 0.27 + 47, 256))"

